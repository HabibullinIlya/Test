pipline{
    agent any
    stages{
        stage('Build'){
            steps{
                sh "echo BUILD"
                sh "chmod +x"
                sh "./gradlew build"
            }
        }
    }
}